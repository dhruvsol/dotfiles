# ═══════════════════════════════════════════════════════════════
# 🌸 Sakura Night - Hyprlock Configuration
# ═══════════════════════════════════════════════════════════════

# ── General Settings ─────────────────────────────────────────────
general {
    disable_loading_bar = false
    hide_cursor = true
    grace = 0
    no_fade_in = false
    no_fade_out = false
    ignore_empty_input = true
}

# ── Background ───────────────────────────────────────────────────
background {
    monitor =
    path = ~/.config/hypr/bg.jpeg
    blur_passes = 3
    blur_size = 8
    noise = 0.02
    contrast = 0.9
    brightness = 0.6
    vibrancy = 0.2
    vibrancy_darkness = 0.1
}

# ── Time Display ─────────────────────────────────────────────────
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%H:%M")"
    color = rgba(187, 154, 247, 1.0)
    font_size = 120
    font_family = JetBrainsMono Nerd Font Bold
    position = 0, 200
    halign = center
    valign = center
    shadow_passes = 3
    shadow_size = 4
    shadow_color = rgba(0, 0, 0, 0.5)
}

# ── Seconds (smaller, subtle) ────────────────────────────────────
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%S")"
    color = rgba(187, 154, 247, 0.5)
    font_size = 40
    font_family = JetBrainsMono Nerd Font
    position = 180, 180
    halign = center
    valign = center
}

# ── Date Display ─────────────────────────────────────────────────
label {
    monitor =
    text = cmd[update:60000] echo "$(date +"%A, %B %d")"
    color = rgba(192, 202, 245, 0.9)
    font_size = 24
    font_family = JetBrainsMono Nerd Font
    position = 0, 80
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 3
    shadow_color = rgba(0, 0, 0, 0.4)
}

# ── Greeting ─────────────────────────────────────────────────────
label {
    monitor =
    text = cmd[update:3600000] echo "Welcome back, $USER"
    color = rgba(125, 207, 255, 0.8)
    font_size = 16
    font_family = JetBrainsMono Nerd Font
    position = 0, -80
    halign = center
    valign = center
}

# ── Password Input ───────────────────────────────────────────────
input-field {
    monitor =
    size = 300, 50
    outline_thickness = 2
    dots_size = 0.3
    dots_spacing = 0.2
    dots_center = true
    dots_rounding = -1
    outer_color = rgba(187, 154, 247, 0.4)
    inner_color = rgba(21, 20, 27, 0.9)
    font_color = rgba(192, 202, 245, 1.0)
    fade_on_empty = false
    fade_timeout = 1000
    placeholder_text = <span foreground="##565f89">󰌾  Enter Password...</span>
    hide_input = false
    rounding = 15
    check_color = rgba(158, 206, 106, 1.0)
    fail_color = rgba(247, 118, 142, 1.0)
    fail_text = <span foreground="##f7768e">󰅜  $FAIL ($ATTEMPTS)</span>
    fail_timeout = 2000
    fail_transition = 300
    capslock_color = rgba(224, 175, 104, 1.0)
    numlock_color = -1
    bothlock_color = -1
    invert_numlock = false
    swap_font_color = false
    position = 0, -150
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 3
    shadow_color = rgba(0, 0, 0, 0.3)
}

# ── Battery Indicator ────────────────────────────────────────────
label {
    monitor =
    text = cmd[update:10000] echo "$(cat /sys/class/power_supply/BAT*/capacity 2>/dev/null || echo "")%" | grep -v "^%$" || echo ""
    color = rgba(158, 206, 106, 0.8)
    font_size = 14
    font_family = JetBrainsMono Nerd Font
    position = -30, 30
    halign = right
    valign = top
}

# ── Battery Icon ─────────────────────────────────────────────────
label {
    monitor =
    text = cmd[update:10000] cat /sys/class/power_supply/BAT*/capacity 2>/dev/null | awk '{if($1>=90)print "󰁹";else if($1>=70)print "󰂀";else if($1>=50)print "󰁾";else if($1>=30)print "󰁼";else if($1>=15)print "󰁺";else print "󰂎"}' || echo ""
    color = rgba(158, 206, 106, 0.8)
    font_size = 18
    font_family = JetBrainsMono Nerd Font
    position = -80, 30
    halign = right
    valign = top
}

# ── Keyboard Layout ──────────────────────────────────────────────
label {
    monitor =
    text = cmd[update:1000] hyprctl devices -j 2>/dev/null | jq -r '.keyboards[0].active_keymap // empty' | cut -c1-2 | tr '[:lower:]' '[:upper:]' || echo ""
    color = rgba(192, 202, 245, 0.6)
    font_size = 12
    font_family = JetBrainsMono Nerd Font
    position = 30, 30
    halign = left
    valign = top
}

# ── Decorative Sakura ────────────────────────────────────────────
label {
    monitor =
    text = 🌸
    font_size = 30
    position = -50, -30
    halign = right
    valign = bottom
}

label {
    monitor =
    text = 🌸
    font_size = 20
    position = -90, -50
    halign = right
    valign = bottom
}

label {
    monitor =
    text = 🌸
    font_size = 25
    position = 50, -40
    halign = left
    valign = bottom
}

# ── Lock Hint ────────────────────────────────────────────────────
label {
    monitor =
    text = Press ESC to clear • Enter to unlock
    color = rgba(86, 95, 137, 0.6)
    font_size = 11
    font_family = JetBrainsMono Nerd Font
    position = 0, -220
    halign = center
    valign = center
}

# ── System Info (subtle) ─────────────────────────────────────────
label {
    monitor =
    text = cmd[update:3600000] echo "$(uname -n)"
    color = rgba(86, 95, 137, 0.5)
    font_size = 10
    font_family = JetBrainsMono Nerd Font
    position = 30, -30
    halign = left
    valign = bottom
}

